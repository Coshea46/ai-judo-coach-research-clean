say that there was 2 different approaches to the type of videos used as data for this approach

- initial approach: use videos that show a highlight throws of one throw only (say this gave matches_by_throw.csv)
- revised approach: use videos that are just of full fights (gives a mix of types of throws) (say this gave full_matches_mixed.csv)


Need to say that the sets of videos for both of the types of videos are disjoint



This is what the README used to say:

# Judo-Throw-Predictor
A time series analysis model predicting a probability distribution over the next possible actions for each player in a judo match given the last 5 seconds of fighting
